apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-native-mini-chart.fullname" . }}
  labels:
    {{- include "cloud-native-mini-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloud-native-mini-chart.selectorLabels" . | nindent 4 }}
