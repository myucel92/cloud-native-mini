apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-native-mini-ingress
  namespace: cloud-mini
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: cloud-mini.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cloud-native-mini-service
                port:
                  number: 80