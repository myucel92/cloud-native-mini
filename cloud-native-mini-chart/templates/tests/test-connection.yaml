apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-native-mini-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cloud-native-mini-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloud-native-mini-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
